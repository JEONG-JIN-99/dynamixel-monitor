# 이상진단 알림

## 동작
- /alerts 페이지와 사이드바 알림 배지는 각 샘플의 diagnosis를 사용한다.
- 모터·버스·실행·서버/소스 세션·이상 코드별로 한 번의 발생을 한 건으로 기록한다.
- 같은 이상이 지속되면 마지막 이상 확인 시각만 갱신한다.
- 유효한 다음 판정에서 사라진 코드는 해제한다. 해제 후 재발은 새 알림이다.
- 미수신, null, waiting, 잘못된 판정은 해제 근거가 아니다.
- 서버 연결/수집 상태가 유효하지 않거나 세션이 바뀌거나 모터 판정이 미확인일 때, 미해제 기록을 상태 확인 대기로 표시한다.
- 조회하지 않은 발생 건수가 사이드바 배지에 표시된다. 100건 이상은 99+로 표시하고 접근성 라벨에는 실제 수를 제공한다.
- 알림 페이지가 보이면 현재 소스의 기록 전체를 읽음 처리한다. 이 페이지를 보고 있는 동안 도착한 알림도 읽음 처리한다. 숨겨진 탭에서는 읽음 처리하지 않는다.
- 읽음과 이상 해제는 별개이다. 읽거나 정상 복귀해도 발생 기록은 삭제하지 않는다.
- 최신 발생순 목록, 이상 종류/모터/ID/버스 검색, 상태 필터, 25건 단위 페이지를 제공한다.

## 백엔드 계약
기존 schemaVersion:1 snapshot/reset/samples 통신은 변경하지 않았다.
샘플은 식별 정보, seq, elapsedMs, timestamp(없으면 receivedAt)와 다음 진단을 포함한다.

정상: {"state":"normal","codes":[]}
이상: {"state":"fault","codes":["overload","friction"]}
대기: {"state":"waiting","codes":[]}

**codes는 해당 모터의 현재 활성 이상 전체 목록이다. 추가된 코드만 보내는 변경분이 아니다.**
동시 발생한 코드는 각각 알림 한 건을 만든다. 알려지지 않은 코드는 원래 코드와 함께 미분류 이상으로 표시한다.
하드웨어 오류, 실험 조건, 그래프 값에서 알고리즘 판정을 추측하지 않는다.

재연결 시 같은 서버/소스/실행 ID와 증가하는 seq를 유지해야 같은 기록으로 이어진다.
저장한 체크포인트 이전의 snapshot 재전송은 중복 알림을 만들지 않는다.
서버/소스/실행 ID가 바뀌면 별도 진단 구간으로 취급하며 이전 미해제 알림은 확인 대기로 남는다.
여러 측정이 한 메시지에 담겨도 모든 판정을 처리한다. 그래프의 최근 60초 보관과 알림 기록은 독립적이다.

## 저장 범위
실제 데이터 모드 기록, 읽음 상태, 체크포인트는 localStorage의 motor-dashboard.diagnosis-alerts.v1에 저장한다.
단일 대시보드 탭 사용을 기준으로 하며 계정/기기/동시 탭 간 읽음 동기화는 구현하지 않았다.
가상 데이터 기록은 메모리에 분리해서 보관하고 새 시연을 시작하면 초기화한다.
저장 실패 시 화면은 계속 동작하고 재접속 시 기록이 보존되지 않을 수 있음을 안내한다.
브라우저 데이터 삭제 시 기록도 삭제된다. 서버에 과거 알림을 저장하거나 조회하는 API는 추가하지 않았다.
화면을 닫은 동안의 과거 이상은 재연결 snapshot에 포함된 측정 이력 범위에서만 복구할 수 있다.

## 구현 및 검증
- services/alerts.ts: 진단 전이, 중복 방지, 저장 데이터 검증
- stores/alertStore.ts: 미읽음 수, 읽음 처리, 저장 및 가상 데이터 격리
- stores/motorStore.ts: 수신된 snapshot/samples를 알림 처리에 전달
- views/AlertsView.vue, styles/alerts.css: 알림 화면
- components/layout/AppSidebar.vue: 배지
- services/alerts.test.ts: 지속·재발·일부 해제·진단 미수신·다중 모터·순서·복원 검증
- e2e/alerts.spec.ts: 실제 WebSocket 형식 입력, 읽음/저장/소스 전환, 저장 실패, 검색/필터 및 1440/900/390 화면 검증

실제 모터 제어나 백엔드 진단 알고리즘은 변경하지 않았다.
