# Device-free CSV demonstration, isolated from actual experiment files.
host = "127.0.0.1"
port = 8001
manifest_path = "../runtime/demo/current_experiment.json"
data_root = "../runtime/demo"
poll_interval_sec = 0.1
retention_sec = 60
max_samples = 12000
subscriber_queue_size = 32
